mutation ActivateOutreachCampaign($id: ID!, $active: Boolean!) {
  updateOutreachCampaignActivity(input: {
    id: $id,
    active: $active
  }) {
    outreachCampaign {
      id
      active
    }
    errors
  }
}
